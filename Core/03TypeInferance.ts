let a:number; // this var will only take number this is Type annotations

let b; // this will take any element and be its type after getting the element this is type inferance

let c: number | string | boolean; // this will take number,string or boolean elements this called union  