// Declare variables of type string, number, and boolean. Assign values to them.
let nm : string = "Asad Hussain"
let num : number = 19
let isEligible : boolean = false

if(num>18){
    isEligible = true
    if(isEligible){
        console.log(`${nm} is ${num} years old he is eligible`);
        
    }
}